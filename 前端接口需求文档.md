# 校园交易平台 - 前端接口需求文档

**项目名称**: E-tradeAndroid  
**文档版本**: v1.0  
**生成日期**: 2026-06-04  
**说明**: 本文档列出前端项目所有需要后端提供的API接口，包括请求方式、路径、参数和返回数据格式。

---

## 📋 目录

- [一、用户模块](#一用户模块)
- [二、商品模块](#二商品模块)
- [三、订单模块](#三订单模块)
- [四、分类模块](#四分类模块)
- [五、消息模块](#五消息模块)
- [六、通用说明](#六通用说明)

---

## 一、用户模块

### 1.1 用户注册

**接口路径**: `POST /api/user/register`  
**使用位置**: `LoginActivity.java` (doRegister方法)  
**认证要求**: 无需登录  

**请求参数** (JSON Body):
```json
{
  "username": "张三",
  "password": "123456",
  "phone": "13800138000"
}
```

**字段说明**:
- `username`: 用户名，必填
- `password`: 密码，必填
- `phone`: 手机号，必填

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": <用户ID>,
  "timestamp": 1715600000000
}
```

---

### 1.2 用户登录

**接口路径**: `POST /api/user/login`  
**使用位置**: `LoginActivity.java` (doLogin方法)  
**认证要求**: 无需登录  

**请求参数** (JSON Body):
```json
{
  "phone": "13800138000",
  "password": "123456"
}
```

**字段说明**:
- `phone`: 手机号，必填
- `password`: 密码，必填

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "username": "张三",
    "phone": "13800138000",
    "avatar": "头像URL",
    "status": 1,
    "isAuth": 0,
    "creditScore": 100,
    "studentId": "2307010301",
    "realName": "张三",
    "createTime": "2024-01-01T...",
    "updateTime": "2024-01-01T..."
  },
  "timestamp": 1715600000000
}
```

**说明**: 登录成功后，前端会保存userId到本地存储。

---

### 1.3 退出登录

**接口路径**: `POST /api/user/logout`  
**使用位置**: `MyProfileActivity.java` (doLogout方法)  
**认证要求**: 需登录  

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

---

### 1.4 获取当前用户信息

**接口路径**: `GET /api/user/current`  
**使用位置**: 
- `MyProfileActivity.java` (loadUserProfile方法)
- `CreditActivity.java` (getUserCredit方法)

**认证要求**: 需登录  

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "username": "张三",
    "phone": "13800138000",
    "avatar": "头像URL",
    "status": 1,
    "isAuth": 0,
    "creditScore": 100,
    "studentId": "2307010301",
    "realName": "张三",
    "createTime": "2024-01-01T...",
    "updateTime": "2024-01-01T..."
  },
  "timestamp": 1715600000000
}
```

**说明**: 
- MyProfileActivity用于显示用户个人资料
- CreditActivity用于获取信用分信息

---

### 1.5 实名认证

**接口路径**: `POST /api/user/verify`  
**使用位置**: `MyProfileActivity.java` (submitRealnameAuth方法)  
**认证要求**: 需登录  

**请求参数** (JSON Body):
```json
{
  "studentId": "2307010301",
  "realName": "张三"
}
```

**字段说明**:
- `studentId`: 学号，必填
- `realName`: 真实姓名，必填

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

---

### 1.6 修改个人资料

**接口路径**: `PUT /api/user/profile`  
**使用位置**: `MyProfileActivity.java` (updateProfile方法)  
**认证要求**: 需登录  

**请求参数** (JSON Body):
```json
{
  "username": "新昵称"
}
```

**字段说明**:
- `username`: 新用户名，必填，2-20位

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

---

### 1.7 上传头像

**接口路径**: `POST /api/user/avatar`  
**使用位置**: `MyProfileActivity.java` (uploadAvatar方法)  
**认证要求**: 需登录  

**请求方式**: `multipart/form-data`

**请求参数**:
- `file`: 图片文件（必填）

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": "头像URL字符串",
  "timestamp": 1715600000000
}
```

---

### 1.8 获取用户信息（根据ID）

**接口路径**: `GET /api/user/info/{userId}`  
**使用位置**: `ProductDetailActivity.java` (loadSellerInfo方法)  
**认证要求**: 无需登录  

**路径参数**:
- `userId`: 用户ID

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "username": "张三",
    "avatar": "头像URL",
    "isAuth": 1,
    "creditScore": 100
  },
  "timestamp": 1715600000000
}
```

**说明**: 用于在商品详情页显示卖家信息。

---

## 二、商品模块

### 2.1 发布商品

**接口路径**: `POST /api/product/publish`  
**使用位置**: `PublishActivity.java` (submitPublish方法)  
**认证要求**: 需登录  

**注意**: 根据后端文档，此接口应使用 `multipart/form-data` 方式提交，但当前前端使用JSON方式。建议后端同时支持两种方式，或前端改为FormData方式。

**请求参数** (当前使用JSON):
```json
{
  "name": "二手手机",
  "price": 999.00,
  "stock": 5,
  "description": "九成新，功能完好"
}
```

**字段说明**:
- `name`: 商品名称，必填，最长50字
- `price`: 价格，必填，必须>0
- `stock`: 库存，必填，至少1
- `description`: 描述，选填，最长500字

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": <商品ID>,
  "timestamp": 1715600000000
}
```

---

### 2.2 商品列表

**接口路径**: `GET /api/product/list?page=1&size=20`  
**使用位置**: `MainActivity.java` (loadProducts方法)  
**认证要求**: 无需登录  

**查询参数**:
- `page`: 页码，默认1
- `size`: 每页条数，默认20

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "sellerId": 1,
      "categoryId": 2,
      "name": "二手手机",
      "price": 999.00,
      "stock": 5,
      "description": "九成新...",
      "images": ["url1", "url2"],
      "mainImage": "封面图URL",
      "status": 1,
      "viewCount": 120,
      "createTime": "2024-01-01T..."
    }
  ],
  "timestamp": 1715600000000
}
```

**说明**: 仅返回 status=1(在售)的商品，按创建时间倒序。

---

### 2.3 商品详情

**接口路径**: `GET /api/product/detail/{productId}`  
**使用位置**: 
- `ProductDetailActivity.java` (loadProductDetail方法)
- `EditProductActivity.java` (loadProductDetail方法)

**认证要求**: 无需登录  

**路径参数**:
- `productId`: 商品ID

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "sellerId": 1,
    "categoryId": 2,
    "name": "二手手机",
    "price": 999.00,
    "stock": 5,
    "description": "九成新...",
    "images": ["url1", "url2"],
    "mainImage": "封面图URL",
    "status": 1,
    "viewCount": 120,
    "createTime": "2024-01-01T..."
  },
  "timestamp": 1715600000000
}
```

**说明**: 每次访问会自动增加浏览量(view_count+1)。

---

### 2.4 我的商品

**接口路径**: `GET /api/product/my`  
**使用位置**: `MyProductsActivity.java` (loadMyProducts方法)  
**认证要求**: 需登录  

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [<商品列表>],
  "timestamp": 1715600000000
}
```

**说明**: 查询当前登录用户发布的所有商品。

---

### 2.5 更新商品

**接口路径**: `POST /api/product/update/{productId}`  
**使用位置**: `EditProductActivity.java` (updateProduct方法)  
**认证要求**: 需登录（仅卖家本人）  

**路径参数**:
- `productId`: 商品ID

**请求参数** (JSON Body):
```json
{
  "name": "二手手机",
  "price": 999.00,
  "stock": 5,
  "description": "九成新...",
  "categoryId": 2
}
```

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 更新后商品 status 重新置为 0(待审核)。

---

### 2.6 下架商品

**接口路径**: `PUT /api/product/offline/{productId}`  
**使用位置**: `EditProductActivity.java` (offshelfProduct方法)  
**认证要求**: 需登录（仅卖家本人）  

**路径参数**:
- `productId`: 商品ID

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 将商品 status 置为 2(已下架)。

---

### 2.7 删除商品

**接口路径**: `DELETE /api/product/delete/{productId}`  
**使用位置**: `EditProductActivity.java` (deleteProduct方法)  
**认证要求**: 需登录（仅卖家本人）  

**路径参数**:
- `productId`: 商品ID

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 物理删除商品记录。

---

### 2.8 搜索商品

**接口路径**: `GET /api/product/search?keyword=手机&categoryId=1&page=1&size=20`  
**使用位置**: `MainActivity.java` (searchProducts方法)  
**认证要求**: 无需登录  

**查询参数**:
- `keyword`: 搜索关键词，选填（模糊匹配商品名称）
- `categoryId`: 分类ID，选填
- `page`: 页码，默认1
- `size`: 每页条数，默认20

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [<商品列表>],
  "timestamp": 1715600000000
}
```

**说明**: 仅搜索 status=1(在售)的商品。

---

## 三、订单模块

### 3.1 创建订单

**接口路径**: `POST /api/v1/trade/order`  
**使用位置**: 
- `ProductDetailActivity.java` (createOrder方法)
- `TradeInfoDialog.java` (create方法)

**认证要求**: 需登录  

**请求参数** (JSON Body):
```json
{
  "productId": 1,
  "quantity": 1,
  "tradeType": 1,
  "payType": 3,
  "meetingTime": "2025-05-20 15:00",
  "meetingLocation": "二食堂门口",
  "addressId": null
}
```

**字段说明**:
- `productId`: 商品ID，必填
- `quantity`: 数量，必填
- `tradeType`: 交易类型，必填（0-线上/快递, 1-线下交易）
- `payType`: 支付方式，必填
- `meetingTime`: 线下交易约定时间，线下交易时必填
- `meetingLocation`: 线下交易约定地点，线下交易时必填
- `addressId`: 收货地址ID，线上交易时必填

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "orderId": "1",
    "orderNo": "xxx"
  },
  "timestamp": 1715600000000
}
```

**说明**: 创建订单后自动扣减库存。不可购买自己的商品。

---

### 3.2 订单详情

**接口路径**: `GET /api/v1/trade/order/{orderId}`  
**使用位置**: 
- `OrderActivity.java` (loadOrderDetail方法)
- `OrderDetailActivity.java` (loadDetail方法)

**认证要求**: 需登录  

**路径参数**:
- `orderId`: 订单ID

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "orderNo": "xxx",
    "status": 1,
    "quantity": 1,
    "totalAmount": 999.00,
    "createTime": "2024-01-01T...",
    "payTime": "2024-01-01T...",
    "completeTime": "2024-01-01T...",
    "productId": 1,
    "productName": "手机",
    "productPriceAtOrder": 999.00,
    "productImage": "url",
    "sellerId": 2,
    "sellerName": "李四",
    "sellerAvatar": "url",
    "buyerId": 1,
    "buyerName": "张三",
    "buyerAvatar": "url",
    "receiverName": "张三",
    "receiverPhone": "138...",
    "receiverAddress": "地址",
    "meetingTime": "2025-05-20 15:00",
    "meetingLocation": "二食堂门口"
  },
  "timestamp": 1715600000000
}
```

**说明**: 仅买家和卖家可查看。

---

### 3.3 取消订单

**接口路径**: `PUT /api/v1/trade/order/{orderId}/cancel`  
**使用位置**: 
- `OrderActivity.java` (cancelOrder方法)
- `OrderDetailActivity.java` (updateOrder方法)

**认证要求**: 需登录（仅买家可操作）  

**路径参数**:
- `orderId`: 订单ID

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 订单状态→4(已取消)，恢复库存。

---

### 3.4 确认订单/付款

**接口路径**: `PUT /api/v1/trade/order/{orderId}/confirm`  
**使用位置**: 
- `OrderActivity.java` (confirmOrder方法)
- `OrderDetailActivity.java` (updateOrder方法)

**认证要求**: 需登录（仅买家）  

**路径参数**:
- `orderId`: 订单ID

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 订单状态→1(已支付/交易中)。

---

### 3.5 发货/交付

**接口路径**: `PUT /api/v1/trade/order/{orderId}/deliver`  
**使用位置**: 
- `OrderActivity.java` (deliverOrder方法)
- `OrderDetailActivity.java` (updateOrder方法)

**认证要求**: 需登录（仅卖家）  

**路径参数**:
- `orderId`: 订单ID

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 订单状态→2(已发货/已交付)。

---

### 3.6 确认收货

**接口路径**: `PUT /api/v1/trade/order/{orderId}/receive`  
**使用位置**: 
- `OrderActivity.java` (confirmReceive方法)
- `OrderDetailActivity.java` (updateOrder方法)

**认证要求**: 需登录（仅买家）  

**路径参数**:
- `orderId`: 订单ID

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

**说明**: 订单状态→3(已完成)。

---

### 3.7 买家订单列表

**接口路径**: `GET /api/v1/trade/order/buyer/list?status=1`  
**使用位置**: `OrderListActivity.java` (loadOrderData方法)  
**认证要求**: 需登录  

**查询参数**:
- `status`: 订单状态，选填（不传则查全部）

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [<订单列表>],
  "timestamp": 1715600000000
}
```

**说明**: 返回当前用户作为买家的订单列表。

---

## 四、分类模块

### 4.1 分类列表

**接口路径**: `GET /api/category/list`  
**使用位置**: `MainActivity.java` (loadCategories方法)  
**认证要求**: 无需登录  

**请求参数**: 无

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "name": "电子产品",
      "description": "手机电脑等",
      "icon": "图标URL",
      "parentId": 0,
      "sortOrder": 1,
      "status": 1,
      "createTime": "2024-01-01T..."
    }
  ],
  "timestamp": 1715600000000
}
```

**说明**: 返回全部启用的分类。

---

## 五、消息模块

### 5.1 发送消息

**接口路径**: `POST /api/message/send`  
**使用位置**: `ChatActivity.java` (sendMsg方法)  
**认证要求**: 需登录  

**请求参数** (JSON Body):
```json
{
  "receiverId": 2,
  "productId": 1,
  "content": "你好，我想买",
  "type": 0
}
```

**字段说明**:
- `receiverId`: 接收者用户ID，必填
- `productId`: 关联商品ID，选填
- `content`: 消息内容，必填
- `type`: 消息类型，必填（0-文字消息, 1-图片URL）

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": null,
  "timestamp": 1715600000000
}
```

---

### 5.2 聊天记录

**接口路径**: `GET /api/message/history?targetUserId=2`  
**使用位置**: `ChatActivity.java` (loadMessage方法)  
**认证要求**: 需登录  

**查询参数**:
- `targetUserId`: 对方用户ID，必填

**返回数据**:
```json
{
  "code": 200,
  "msg": "成功",
  "data": [
    {
      "id": 1,
      "senderId": 1,
      "receiverId": 2,
      "productId": 1,
      "content": "你好，我想买",
      "type": 0,
      "isRead": 0,
      "createTime": "2024-01-01T..."
    }
  ],
  "timestamp": 1715600000000
}
```

**说明**: 返回当前用户与 targetUserId 之间的双向消息记录。

---

### 5.3 会话列表（暂未使用）

**接口路径**: `GET /api/v1/message/sessions`  
**使用位置**: `MessageListActivity.java` (已注释，暂未启用)  
**认证要求**: 需登录  

**说明**: 此接口在前端代码中已被注释，暂时不需要实现。如需实现消息会话列表功能，可提供此接口。

---

## 六、通用说明

### 6.1 统一响应格式

所有接口返回 JSON，结构如下：

```json
{
  "code": 200,
  "msg": "成功",
  "data": <T>,
  "timestamp": 1715600000000
}
```

**字段说明**:
- `code`: 状态码，200表示成功，其他值表示失败
- `msg`: 提示信息
- `data`: 业务数据（类型视接口而定，可能为对象、数组、null或基本类型）
- `timestamp`: 响应时间戳（毫秒）

### 6.2 错误处理

当请求失败时，`code` 不为 200，`data` 通常为 null，`msg` 包含错误信息。

示例：
```json
{
  "code": 401,
  "msg": "未登录或登录已过期",
  "data": null,
  "timestamp": 1715600000000
}
```

### 6.3 认证方式

- **需登录接口**: 需要在请求头中携带 Cookie 或 Token
- **无需登录接口**: 可直接访问

前端使用 OkHttp 的 CookieJar 自动管理 Cookie，后端可通过 Session 或 JWT 进行身份验证。

### 6.4 分页说明

对于列表接口，建议使用以下分页参数：
- `page`: 页码，从1开始
- `size`: 每页条数，默认10或20

返回数据中建议包含分页信息：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "size": 20,
    "pages": 5
  },
  "timestamp": 1715600000000
}
```

### 6.5 文件上传

文件上传接口使用 `multipart/form-data` 格式：
- 头像上传: `POST /api/user/avatar`
- 商品发布（建议）: `POST /api/product/publish`

### 6.6 特殊说明

1. **商品发布接口**: 当前前端使用 JSON 方式提交，但后端文档要求使用 `multipart/form-data`。建议：
   - 方案A: 后端同时支持 JSON 和 FormData 两种方式
   - 方案B: 前端改为使用 MultipartBody 提交

2. **信用分接口**: 前端目前没有独立的信用分查询接口，而是通过 `/api/user/current` 获取用户信息中的 `creditScore` 字段。

3. **订单状态流转**:
   ```
   待付款(0) → [买家付款/confirm] → 待发货(1) → [卖家发货/deliver] → 待收货(2) → [买家收货/receive] → 已完成(3)
   任意非终态 → [买家取消/cancel] → 已取消(4)
   ```

4. **商品状态**:
   - 0: 待审核
   - 1: 在售
   - 2: 已下架
   - 3: 已售出
   - 4: 审核不通过

---

## 📊 接口统计

| 模块 | 接口数量 | 需登录 | 无需登录 |
|------|---------|--------|----------|
| 用户模块 | 8 | 5 | 3 |
| 商品模块 | 8 | 5 | 3 |
| 订单模块 | 7 | 7 | 0 |
| 分类模块 | 1 | 0 | 1 |
| 消息模块 | 2 | 2 | 0 |
| **总计** | **26** | **19** | **7** |

---

## 🔗 相关文件映射

| 接口路径 | 前端文件 | 方法名 |
|---------|---------|--------|
| POST /api/user/register | LoginActivity.java | doRegister() |
| POST /api/user/login | LoginActivity.java | doLogin() |
| POST /api/user/logout | MyProfileActivity.java | doLogout() |
| GET /api/user/current | MyProfileActivity.java, CreditActivity.java | loadUserProfile(), getUserCredit() |
| POST /api/user/verify | MyProfileActivity.java | submitRealnameAuth() |
| PUT /api/user/profile | MyProfileActivity.java | updateProfile() |
| POST /api/user/avatar | MyProfileActivity.java | uploadAvatar() |
| GET /api/user/info/{id} | ProductDetailActivity.java | loadSellerInfo() |
| POST /api/product/publish | PublishActivity.java | submitPublish() |
| GET /api/product/list | MainActivity.java | loadProducts() |
| GET /api/product/detail/{id} | ProductDetailActivity.java, EditProductActivity.java | loadProductDetail() |
| GET /api/product/my | MyProductsActivity.java | loadMyProducts() |
| POST /api/product/update/{id} | EditProductActivity.java | updateProduct() |
| PUT /api/product/offline/{id} | EditProductActivity.java | offshelfProduct() |
| DELETE /api/product/delete/{id} | EditProductActivity.java | deleteProduct() |
| GET /api/product/search | MainActivity.java | searchProducts() |
| POST /api/v1/trade/order | ProductDetailActivity.java, TradeInfoDialog.java | createOrder(), create() |
| GET /api/v1/trade/order/{id} | OrderActivity.java, OrderDetailActivity.java | loadOrderDetail(), loadDetail() |
| PUT /api/v1/trade/order/{id}/cancel | OrderActivity.java, OrderDetailActivity.java | cancelOrder(), updateOrder() |
| PUT /api/v1/trade/order/{id}/confirm | OrderActivity.java, OrderDetailActivity.java | confirmOrder(), updateOrder() |
| PUT /api/v1/trade/order/{id}/deliver | OrderActivity.java, OrderDetailActivity.java | deliverOrder(), updateOrder() |
| PUT /api/v1/trade/order/{id}/receive | OrderActivity.java, OrderDetailActivity.java | confirmReceive(), updateOrder() |
| GET /api/v1/trade/order/buyer/list | OrderListActivity.java | loadOrderData() |
| GET /api/category/list | MainActivity.java | loadCategories() |
| POST /api/message/send | ChatActivity.java | sendMsg() |
| GET /api/message/history | ChatActivity.java | loadMessage() |

---

**文档结束**

如有疑问或需要补充，请联系前端开发团队。
