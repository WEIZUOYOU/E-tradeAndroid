<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中状态：主绿色 -->
    <item android:color="@color/tag_selected_bg" android:state_checked="true" />
    <item android:color="@color/tag_selected_bg" android:state_selected="true" />
    <!-- 默认状态：浅灰绿 -->
    <item android:color="@color/tag_default_bg" />
</selector>